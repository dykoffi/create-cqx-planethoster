{
  "dev": {
    "pre_build":"yarn test --ci",
    "build": "cqx build",
    "artifact": "build",
    "deploy_on": {
      "hostname": "node52-eu.n0c.com",
      "port": "5022",
      "path": "<%= appPath %>",
      "cmd": "source <%= appEnv %> && yarn && echo date -u >> tmp/restart.txt"
    },
    "post_deploy": "rm -rdf build",
    "url": "<%= appDomain %>"
  }
}